import NotFoundPage from "@/components/ui/page-not-found";

export const metadata = {
  title: "404 — Page Not Found | Frontier Biomed",
  description: "The page you requested could not be found.",
};

export default function NotFound() {
  return <NotFoundPage />;
}
