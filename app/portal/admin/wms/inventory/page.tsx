import { WmsInventoryAlerts } from "@/components/portal/admin/wms/WmsInventoryAlerts";

export const metadata = {
  title: "Inventory Alerts — WMS",
};

export default function WmsInventoryPage() {
  return <WmsInventoryAlerts />;
}
