import { AdminApprovalQueue } from "@/components/portal/admin/approvals/AdminApprovalQueue";

export const metadata = {
  title: "Approval Queue — Admin portal",
};

export default function AdminApprovalsPage() {
  return <AdminApprovalQueue />;
}
