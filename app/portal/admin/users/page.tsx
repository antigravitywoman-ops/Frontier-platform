import { AdminUserManagement } from "@/components/portal/admin/users/AdminUserManagement";

export const metadata = {
  title: "Users — Admin portal",
};

export default function AdminUsersPage() {
  return <AdminUserManagement />;
}
