import { HelpSupport } from "@/components/portal/provider/help/HelpSupport";

export const metadata = {
  title: "Help & support — Provider portal",
  description: "Knowledge base, support tickets, and contact options.",
};

export default function HelpSupportPage() {
  return <HelpSupport />;
}
