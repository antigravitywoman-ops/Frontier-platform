import { ProviderSettings } from "@/components/portal/provider/settings/ProviderSettings";

export const metadata = {
  title: "Settings — Provider portal",
  description: "Manage practice info, branding, banking, notifications, and security.",
};

export default function ProviderSettingsPage() {
  return <ProviderSettings />;
}
